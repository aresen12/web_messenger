status chat
1 - действующий
2 - deleted chat
3 - block chat


type message
1 - обычное
2 - эмоции
Схема получения сообщений
    через сокет

gener_html(id, text, time, html_m, file, other);
set_recipient(id_chat, is_primary, name, status);




    global_menu.innerHTML += `<div id="meet"></div>`

const domain = 'meet.jit.si';
const options = {
    roomName: 'fhghjkjhgfddeyuoiouyutt5467890kazbek' + document.getElementById("chat_id").value,
    width: "100%",
    height: "70vh",
    parentNode: document.querySelector('#meet'),
    userInfo: {
//    добавить гыуктфьу
        email: '',
        displayName: document.getElementById("name_user").textContent
    }
};
const api = new JitsiMeetExternalAPI(domain, options);
